/** App paths — single source for links and redirects. */

export const routes = {
  home: "/",
  howItWorks: "/how-it-works",
  login: "/login",
  register: "/register",
  dashboard: "/dashboard",
  dashboardForm: "/dashboard/form",
} as const;
